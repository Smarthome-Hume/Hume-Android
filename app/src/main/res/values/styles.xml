<resources>
    <!--
        The app has one fixed light look, so the platform theme must not follow
        the system dark mode: a DayNight parent turned the window background and
        the status bar icons dark behind the light Compose surfaces.
        Bars are transparent because Android 15 always draws edge to edge; the
        insets are handled in HumeRootScreen.
    -->
    <style name="Theme.Hume" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/hume_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
    </style>
</resources>
